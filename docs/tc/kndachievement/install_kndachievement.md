---
title: 安裝
order: 1
---

# 安裝


## 安裝步驟

1. 將 `KonadoAchievement` 插件複製到 Godot 專案的 `addons` 目錄下
2. 在 Godot 編輯器中，進入 `專案 -> 專案設定 -> 外掛程式`，啟用 `KonadoAchievement` 插件

## 自動載入

KND_AchievementManager 會將 KND_AchievementManager 新增到專案的 `root` 節點下，無需手動載入。