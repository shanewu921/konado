name: GDS Check

on:
  workflow_dispatch:

jobs:
  static-checks:
    name: 'Static checks'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: Scony/godot-gdscript-toolkit@master
    - run: gdlint .